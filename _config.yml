# destination: site_staging
timezone: "Asia/Singapore"

# Site settings
title: AI-driven Business Transformation | Consulting & Development
# Short label for the navbar. site.title is a consulting strapline that wraps
# onto three lines on a phone; it is still used for <title> and metadata.
brand: AISeng Tech
SEOTitle: AI Consultant | Enterprise Agentic AI | Tokenization payment
header-img: image/rawa-sun-falling2-a
email: goseng123@gmail.com
author: Sheng Gao
# Used by jekyll-seo-tag for <meta name="description">, og:description and
# twitter:description on any page that does not supply its own. It was
# commented out, so the feed had an empty <description> and there was no
# site-level fallback anywhere.
description: "Daily briefs and analysis on enterprise agentic AI, fintech and payments, tokenization, and AI governance, plus consulting and development services."
keyword: Enterprise Agentic AI, digital Transformation, Tokenization payment
# MUST match CNAME (aisengtech.com). This said https://www.aisengtech.com,
# but www 301-redirects to the apex, so every canonical URL, sitemap entry
# and feed link pointed at a redirect. robots.txt already used the apex.
url: "https://aisengtech.com"
github_repo: "https://github.com/aigaosheng/aigaosheng.github.io.git"

# Navigation bar, in order.
#
# The nav used to loop over site.pages and print every page that had a title,
# so link order was filesystem-dependent and any new page joined the bar
# automatically -- which would now mean all 15 series pages. Listing the links
# here keeps the bar deliberate. Peripheral pages (Resume, Privacy & Support)
# moved to the footer rather than disappearing.
nav:
  - title: Home
    url: /
  - title: Series
    url: /series/
  - title: Tags
    url: /tags/
  - title: About
    url: /about/
  - title: Services
    url: "/products&services/"
  - title: Search
    url: /search/

# Secondary links, footer only.
nav_secondary:
  - title: Resume
    url: /resume/
  - title: Privacy & Support
    url: "/Privacy&Support/"

# Sidebar settings
sidebar: true                           # whether or not using Sidebar.
sidebar-about-description: Artificial intelligence, Agentic AI, machine learning, data science, Expert Developers
sidebar-avatar: /image/gs.jpg     

# SNS settings
linkedin_username:    dlsheng 
github_username:    aigaosheng


# Build settings
# from 2016, 'pygments' is unsupported on GitHub Pages. Use 'rouge' for highlighting instead.
permalink: pretty
paginate: 12
exclude: ["less","node_modules","Gruntfile.js","package.json","README.md", "draft", "docs", "img"]
anchorjs: true 



# Gems
# from PR#40, to support local preview for Jekyll 3.0
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-paginate


# Markdown settings
# replace redcarpet to kramdown,
# although redcarpet can auto highlight code, the lack of header-id make the catalog impossible, so I switch to kramdown
# document: http://jekyllrb.com/docs/configuration/#kramdown
markdown: kramdown
highlighter: rouge
kramdown:
  input: GFM                            # use Github Flavored Markdown !important



# 评论系统
#Disqus（https://disqus.com/）
# disqus_username: aigaosheng-github-io
# disqus:
#   enable: False    #是否开启Disqus评论
#   username: aigaosheng-github-io

# Default og:image / twitter:image for every page.
#
# This has to go through front matter defaults, not a top-level `image:` key:
# jekyll-seo-tag 2.8.0 reads page["image"] only (image_drop.rb) and has no
# site-level fallback, so a bare site.image is silently ignored. Setting it as
# a default gives every document a page.image that the plugin does pick up,
# and any post can still override it in its own front matter.
#
# Supplying an image also upgrades twitter:card from "summary" to
# "summary_large_image" and adds twitter:image (see the plugin's template.html).
# Crawlers fetch this server-side, so its file size does not affect page weight.
defaults:
  - scope:
      path: ""
    values:
      image: /image/rawa-sun-falling3.jpg
      # Scrim opacity behind the white hero text. The hero photo is bright, so
      # without this the title and subheading were unreadable over the sky.
      # Override per page/post, or set 0 to remove.
      header-mask: 0.45


# Featured Tags
featured-tags: true                     # 是否使用首页标签
# A tag must be used MORE than this many times to appear in the sidebar cloud.
# This was 1, which admitted every tag used twice or more -- 786 links rendered
# into the sidebar of all ~2,000 pages, making each page ~340 KB of boilerplate
# around ~2.4 KB of article. 24 keeps the cloud at roughly 35 meaningful tags.
featured-condition-size: 24
# Hard ceiling on sidebar tag links, so the cloud cannot silently grow back
# as more posts are added and more tags cross the threshold above.
featured-tags-max: 40



# Progressive Web Apps
chrome-tab-theme-color: "#000000"
service-worker: true


# Friends
friends: [
    {
        title: "",
        href: ""
    },
]
